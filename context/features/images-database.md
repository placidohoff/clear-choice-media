# Images database

## Goals
Use Cloudinary as the media source for production assets, with a future metadata model to store image URLs, alt text, and categories.

- Cloudinary needs to be able to be the source where all the images are pulled from. The user must be able to upload and display new images.